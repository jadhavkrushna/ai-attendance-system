# Install with scripts/setup_environment.ps1 on Windows so face-recognition
# uses the prebuilt dlib-bin package instead of attempting a local dlib build.

# Utilities
numpy
pandas

# Face recognition
scikit-learn
dlib-bin==20.0.1; platform_system == "Windows"
face-recognition-models==0.3.0
# On Windows this is installed by the setup script with --no-deps because
# dlib-bin supplies the dlib module without satisfying upstream metadata.
face-recognition==1.3.0; platform_system != "Windows"
setuptools<70.0.0

supabase==2.10.0
bcrypt

segno
pillow

# Web application
streamlit

#voice recognition
librosa
resemblyzer
